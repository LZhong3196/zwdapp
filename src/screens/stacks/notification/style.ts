import { Widgets } from "summer";
const {
    color_theme,
    color_base,
    font_size_base,
    color_background
} = Widgets.theme;

export const styles: any = {
    header: {
        backgroundColor: color_theme
    },
};

